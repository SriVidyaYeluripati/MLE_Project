# bomberman_rl
Setup for a project/competition amongst students to train a winning Reinforcement Learning agent for the classic game Bomberman.


Experiment harness: the runners that produced `results/model_linq/` drove the agent through `LQ_*` environment switches. The shipped agent has those values hard-coded, so the harness lives at tag `experiments-v1`.
